library(testthat)
library(osmdatar)
#wd <- getwd ()
#while (length (grep ('osmdatar', getwd ())) > 0) setwd ("..")
#devtools::load_all ('osmdatar', export_all=FALSE)
#setwd (wd)

test_check("osmdatar")
